#------------------------------------------------------------
# Values for this site.
#------------------------------------------------------------

# Domain for searches.
domain: "https://swcarpentry.github.io/sql-novice-survey"

# URL for repository.
repo: "https://github.com/swcarpentry/sql-novice-survey"

# Root URL for lesson below domain.
root: "/sql-novice-survey"

# Overall lesson title.
title: "Using Databases and SQL"

# Contact email address.
email: lessons@software-carpentry.org

# What kind of thing is this ("workshop", "lesson")?
kind: "lesson"

# Which carpentry is this ("swc", "dc")?
carpentry: "swc"

#------------------------------------------------------------
# Generic settings (should not need to change).
#------------------------------------------------------------

# Sites.
amy_site: "https://amy.software-carpentry.org/workshops"
dc_site: "https://datacarpentry.org"
swc_github: "https://github.com/swcarpentry"
swc_site: "https://software-carpentry.org"
template_repo: "https://github.com/swcarpentry/styles"
example_repo: "https://github.com/swcarpentry/sql-novice-survey"
example_site: "https://swcarpentry.github.com/sql-novice-survey"
workshop_repo: "https://github.com/swcarpentry/workshop-template"
workshop_site: "https://swcarpentry.github.io/workshop-template"

# Start time in minutes (540 is 09:00 am)
start_time: 540

# Specify that things in the episodes collection should be output.
collections:
  episodes:
    output: true
    permalink: /:path/
  extras:
    output: true

# Set the default layout for things in the episodes collection.
defaults:
  - scope:
      path: ""
      type: episodes
    values:
      layout: episode

# Files and directories that are not to be copied.
exclude:
  - Makefile
  - bin

# Turn off built-in syntax highlighting.
highlighter: false
